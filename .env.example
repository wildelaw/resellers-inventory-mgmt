# NextAuth JWT signing key — generate with: openssl rand -base64 32
AUTH_SECRET=

# NextAuth base URL — must match your domain exactly (used for Origin/Referer CSRF checks)
AUTH_URL=http://localhost:3000

# Set to false for HTTP-only development (cookies will not be marked Secure)
COOKIE_SECURE=false